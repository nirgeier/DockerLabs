<source>
  @type syslog
  port 5140
  bind 0.0.0.0
  tag system.syslog
</source>

<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match docker.**>
  @type stdout
</match>

<match system.**>
  @type stdout
</match>